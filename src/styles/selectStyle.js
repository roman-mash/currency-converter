const selectStyle = {
  control: (provided) => ({
    ...provided,
    width: '100%',
  }),
  container: (provided) => ({
    ...provided,
    width: '100%',
    minWidth: 400,
    marginBottom: 30,
  }),
};

export default selectStyle;
