const selectForConverter = {
  control: (provided) => ({
    ...provided,
    width: '100%',
  }),
  container: (provided) => ({
    ...provided,
    width: '110px',
  }),
};

export default selectForConverter;
